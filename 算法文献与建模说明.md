# 五核任务划分与调度：文献依据及建模路线

## 问题与边界

第一问的输入是由算子、张量和依赖组成的有向无环图。需要把计算算子组成任务，再将任务安排到五个核上；方案必须保持依赖合法，并受片上存储、数据搬运、同核切换和跨核等待等约束。目标是缩短总完成时间（makespan）。五核求解只使用当前五核图和配置，不读取一至四核的优化结果。官方评测器用于验证方案和报告成绩，不作为训练数据拟合规则。

这里的建模决策是：**先用图结构及硬件代价构造方案，最后用官方评测器验证**。不按 case 编号设规则，不随机生成大量方案，不扫参数组合，也不枚举所有划分。FM 属于局部优化启发式，不是穷举算法；它仍会比较少量有根据的邻近方案。

## 实际借鉴的论文

| 论文 | 原文方法 | 当前代码对应部分 | 需要说明的差距 |
| --- | --- | --- | --- |
| Fiduccia 与 Mattheyses, [*A Linear-Time Heuristic for Improving Network Partitions*](https://limsk.ece.gatech.edu/book/papers/fm.pdf), DAC 1982 | 按移动收益细化网络划分，单轮锁定移动，并保留累计收益最好的前缀 | [`acyclic_fm_refinement.py`](acyclic_fm_refinement.py) 中的边界移动、锁定与最佳前缀回退 | 当前实现每次重算代理目标，**不具备**原论文的线性时间增量收益更新；也只移动当前任务边界上的算子 |
| Popp 等, [*Multilevel Acyclic Hypergraph Partitioning*](https://arxiv.org/abs/2002.02962), 2020 | 用有向超图表示一对多数据依赖，在商图无环约束下做多层划分与细化，以减少通信并改善调度 | [`acyclic_fm_refinement.py`](acyclic_fm_refinement.py) 的张量超边、商图无环检查；[`optimize_fm_5core.py`](optimize_fm_5core.py) 的相邻任务收缩 | 当前仅有浅层收缩和一次细化，**尚不是**论文中的完整多层收缩、初始划分、逐层展开和细化流程 |
| Moreira、Popp、Schulz, [*Graph Partitioning with Acyclicity Constraints*](https://drops.dagstuhl.de/entities/document/10.4230/LIPIcs.SEA.2017.30), SEA 2017 | 论证任务商图无环是划分约束，给出可扩展的启发式 | [`fast_solver.py`](fast_solver.py) 的安全任务合并，以及 FM 移动前的无环检查 | 该论文解决的是图划分；我们的目标还包含核调度与硬件资源限制 |
| Papp 等, [*Efficient Multi-Processor Scheduling in Increasingly Realistic Models*](https://arxiv.org/abs/2404.15246), SPAA 2024 | 在计算 DAG 调度中显式考虑通信、同步和层次结构；结合初始启发式、局部改进与多层方法 | [`adaptive_clustering.py`](adaptive_clustering.py) 的通信感知划分和插入式列表调度；FM 候选也用该调度器构造五核方案 | 论文采用的机器模型与本题不完全相同；当前代理目标还没有充分描述共享带宽争用和等待传播 |
| Moreira、Popp、Schulz, [*Evolutionary Acyclic Graph Partitioning*](https://arxiv.org/abs/1709.08563), 2017 | 多层无环划分，再用演化方法改善通信和负载 | 只参考了“无环划分与负载、通信共同优化”的问题形式 | **没有采用**演化搜索；它不符合当前避免大量候选搜索的取向 |

## 当前算法如何生成方案

1. 从算子—张量—算子的依赖重建计算 DAG，并收缩 COPY 节点上的依赖路径；张量作为超边，其扇出在边界通信代理项中计一次。
2. 从已有五核方案出发，只选真实跨任务依赖上的活跃边界算子。移动后任务不得为空，任务商图必须无环。
3. 用跨任务张量字节数、任务的各流水线负载峰值、近似内存压力给候选移动排序。FM 允许暂时负收益的移动，但只保留整轮中代理目标最好的前缀。
4. 对细化或少量合法收缩后的划分运行依赖感知的插入式列表调度，形成完整的五核方案。
5. 用官方评测器评价有界的最终候选，保留真实 makespan 更小的合法方案。评测次数只是验证和选择少量模型输出，不是搜索空间的主驱动力。

这些代理项不是官方 makespan 的精确公式。例如共享 DDR 带宽会随任务重叠变化，内存占用也受执行顺序影响。因此代理项负责给搜索方向，最终判断仍以官方约束和成绩为准。当前代码的 `memory_weight` 参数没有进入目标公式，不能把它描述为已完成的可调内存权重。

## 对现状的判断与下一步

已有迭代的五核平均加速比从 4.031050 提升到 4.077066，但尚未达到 4.2。仅重复更多邻域尝试的边际收益很小；目前更值得改的是**模型和构造机制**，不是扩大候选数。

建议下一轮按下面的顺序实现和验证：

1. 将任务耗时估计改为“各流水线最大计算负载 + 读写通信 + 等待”，在任务 DAG 上计算关键路径与五核负载下界；用这些量解释一次边界移动对总完成时间的影响。公式只作结构引导，不复制官方评测逻辑。
2. 对张量通信按生产任务和每个消费任务计入读写与扇出，同时用峰值活跃张量估计存储压力。这样比单个“是否跨界”字节总量更接近本题约束。
3. 做真正的**多层算法**：沿安全依赖边收缩，粗层构造划分，逐层展开并在每层只细化关键边界；每层后用五核调度器更新关键路径。限制工作量按图规模增长，不按 case 分数加大搜索。
4. 让收缩与移动优先级由关键路径节省、通信节省和核负载变化共同决定；保留少量结构不同的最终方案供官方验证。若这些机制没有改善整套 100 例的平均分，应分析代理项失配，而不是继续增加评测预算。

文献中的优异改进不能直接外推成本题达到 4.2、4.3 或 4.5；能否达到需要在全部 100 例上验证，并报告改善、退化和内存占用。
